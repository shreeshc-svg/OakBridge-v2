# Runtime dependencies for the Oakbridge backend.
#
# Keep this file and requirements.txt IDENTICAL below the header --
# sanity-check.js enforces it (check: requirements-parity). This is the file
# render.yaml's buildCommand installs, so it is the one production boots from.
#
# Versions are left unpinned so pip picks wheels that exist for your Python
# (works on 3.12, 3.13, 3.14).
#
# Install:  pip install -r requirements-local.txt

fastapi
uvicorn[standard]
motor
pymongo
pydantic[email]
python-dotenv
python-multipart
bcrypt
PyJWT
requests
openpyxl
razorpay
resend
reportlab
boto3>=1.34
pypdfium2
pillow
