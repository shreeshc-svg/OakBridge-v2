# Lean local-dev dependencies for the Oakbridge backend.
# Only the packages the app actually imports to boot & serve — no
# google/grpc/pandas/boto3/stripe/litellm bloat from the Emergent base
# image. Versions are left unpinned so pip picks wheels that exist for
# your Python (works on 3.12, 3.13, 3.14).
#
# Install:  pip install -r requirements-local.txt

fastapi
uvicorn[standard]
motor
pymongo
pydantic[email]
python-dotenv
python-multipart
bcrypt
PyJWT
requests
openpyxl
razorpay
resend
reportlab
boto3>=1.34
