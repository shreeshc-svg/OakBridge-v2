# Runtime dependencies for the Oakbridge backend.
#
# Keep this file and requirements-local.txt IDENTICAL below the header --
# sanity-check.js enforces it (check: requirements-parity). Two files exist
# because render.yaml's buildCommand installs requirements-local.txt while
# README tells developers to install requirements.txt; they must therefore
# describe the same environment. If you add a package, add it to both.
#
# This used to be a 128-line pip freeze inherited from the Emergent base
# image -- litellm, openai, google-generativeai, stripe, pandas, numpy,
# tiktoken, huggingface_hub -- none of which this app imports. Production
# never installed it, so anyone following the README got a different
# environment than the one that actually runs.
#
# Versions are left unpinned so pip picks wheels that exist for your Python
# (works on 3.12, 3.13, 3.14).
#
# Install:  pip install -r requirements.txt

fastapi
uvicorn[standard]
motor
pymongo
pydantic[email]
python-dotenv
python-multipart
bcrypt
PyJWT
requests
openpyxl
razorpay
resend
reportlab
boto3>=1.34
pypdfium2
pillow
